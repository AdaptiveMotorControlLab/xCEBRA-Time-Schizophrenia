# xCEBRA pipeline - pinned direct dependencies
# Generated 2026-07-17T20:59:15 on Python 3.10.12 (Linux-6.8.0-49-generic-x86_64-with-glibc2.35)
# Regenerate by re-running generate_requirements.py
#
# For a bit-for-bit environment, use pip_freeze.txt (full transitive closure).
# torch note: the pin below is the base version; if a '+cuXXX' build was used
#   (see trailing comment), install it from the matching index, e.g.:
#   pip install torch==<ver> --index-url https://download.pytorch.org/whl/cuXXX

cebra==0.6.0a1
joblib==1.5.3
matplotlib==3.10.8
nibabel==5.4.2
nilearn==0.13.1
numpy==1.26.4
pandas==2.3.3
pingouin==0.6.1
scikit-learn==1.7.2
scipy==1.15.3
seaborn==0.13.2
SUITPy==2.0.0
torch==2.4.0+cu121
